# opencode-agents

This repository holds all my agent configurations for my [opencode](https://github.com/anomalyco/opencode) setup.

## Structure

Agent configs are organized by purpose — each agent defines a specialized subagent with its own set of tools and instructions.

## Usage

Agent configs in this repository are referenced by opencode to provide task-specific subagent behaviors.
